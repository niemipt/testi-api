openapi: 3.0.3
info:
  title: My API
  version: '1.0'
  contact:
    name: Pete
    email: petri.niemi@sofigate.com
externalDocs:
  url: https://niemipt.github.io/testi-api/
servers:
  - url: https://httpbin.org
    description: HTTPBin palvelin
paths:
  /get:
    summary: HTTPBin GET
    description: HTTPBin GET resurssi
    get:
      responses:
        '200':
          description: Success
          content:
            'application/json':
              schema:
                $ref: ./schemas/Responses.yaml#/components/schemas/GetResponse
  /post:
    summary: HTTPBin POST
    description: HTTPBIn POST resurssi
    post:
      requestBody:
        content:
          'application/json':
            schema:
              $ref: ./schemas/Requests.yaml#/components/schemas/PostRequest
      responses:
        '200':
          description: Success
          content:
            'application/json':
              schema:
                type: object
components:
  schemas: {}
  securitySchemes:
    BasicAuth:
      type: http
      scheme: basic
      description: |-
        Example: 
        > Authorization: Basic ZGVtbzpwQDU1dzByZA==
security:
  - BasicAuth: []
